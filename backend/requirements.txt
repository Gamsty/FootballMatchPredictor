# Core Flask
Flask==3.1.3
Flask-CORS==6.0.2
Werkzeug==3.1.6

# Database
psycopg2-binary==2.9.11
SQLAlchemy==2.0.48

# ML & Data Science
scikit-learn==1.8.0
xgboost==3.2.0
pandas==3.0.1
numpy==2.4.3
joblib==1.5.3

# API & HTTP
requests==2.32.5
# Cloudflare-aware HTTP for sofascore lineup scraping. Wraps requests with
# the JS-challenge solver so we don't need a headless browser. Pin to a known
# stable version — newer minor releases occasionally drop bypass methods.
cloudscraper==1.2.71

# Fuzzy team-name matching (Odds API ↔ football-data.org naming differences)
rapidfuzz==3.14.5

# Environment
python-dotenv==1.2.2

# Production Server
gunicorn==25.1.0

# Azure (model storage from Blob, Key Vault auth via Managed Identity)
azure-identity==1.21.0
azure-storage-blob==12.26.0

# Observability (Application Insights via OpenTelemetry)
azure-monitor-opentelemetry==1.6.5
opentelemetry-instrumentation-flask==0.51b0
opentelemetry-instrumentation-sqlalchemy==0.51b0
opentelemetry-instrumentation-requests==0.51b0
